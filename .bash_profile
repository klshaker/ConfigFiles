# . means run this as if I were typing. Otherise it will open a new shell 
if [ -f ~/.bash_rc ]; then . ~/.profile;fi
# OSX always opens login shells - bash_profiles run every login shell opening
# Linux doesn't always open login shells - it runs interactive shells 
# bash_profile- login shell
#bashrc - interactive shell 

